# Copy to .env for local dev — never commit .env
# For Modal training runs, secrets come from the Modal secret "llm-api-keys" (already created).
# For local runs, load from AWS SM via: python -c "from src.utils.secrets import load_secrets; load_secrets()"

HF_TOKEN=
NVIDIA_API_KEY=
WANDB_API_KEY=
MODAL_TOKEN_ID=
MODAL_TOKEN_SECRET=

# AWS (temporary SSO creds — refresh from sandbox console as needed)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_SESSION_TOKEN=
AWS_DEFAULT_REGION=us-east-2
